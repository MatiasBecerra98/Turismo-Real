
local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

amazon:
  service: S3
  AWS_KEY: ENV['AWS_KEY']
  AWS_SECRET: ENV['AWS_SECRET']
  BUCKET: ENV['BUCKET']
  REGION: ENV['REGION']
